docker config create phptek_mysite.2 ./mysite2.conf

# open http://localhost/

docker service update phptek_nginx --config-rm phptek_mysite \
	--config-add source=phptek_mysite.2,target=/etc/nginx/conf.d/default.conf

# open http://localhost/

docker service inspect phptek_nginx | grep 'Configs' -A 10

docker service rollback phptek_nginx

# open http://localhost/

docker config create xdebug ./xdebug.ini
docker service update --config-add source=xdebug,target=/usr/local/etc/php/xdebug.ini phptek_php