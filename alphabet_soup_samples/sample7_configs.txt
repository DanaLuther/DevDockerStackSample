docker config create phpworld_mysite.2 ./mysite2.conf

# open http://localhost/

docker service update phpworld_nginx --config-rm phpworld_mysite \
	--config-add source=phpworld_mysite.2,target=/etc/nginx/conf.d/default.conf

# open http://localhost/

docker service inspect phpworld_nginx | grep 'Configs' -A 10

docker service rollback phpworld_nginx

# open http://localhost/

docker config create xdebug ./xdebug.ini
docker service update --config-add source=xdebug,target=/usr/local/etc/php/xdebug.ini phpworld_php